import 'package:flutter/material.dart';

const primaryColor = Color(0xFF68696B);
const secondaryColor = Color(0xFF14171C);
const darkColor = Color(0xFF050A30);
const bodyTextColor = Color(0xFFFFFFFF);
const bgColor = Color(0xFFE8E6E6);
const bgColor2 = Color(0xFFD9D9D9);

const barFontSize = 21.0;
const barFontFamily = 'Inter';
const barFontColor = Color(0xFFFFFFFF);
const barFontColor1 = Color(0xFF7C512C);

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1);
const maxWidth = 900.0;
const maxHeight = 600.0;

const drawerIconColor1 = Color(0xFFFFFFFF);
const drawerIconColor2 = Color(0xFF000000);

const drawerTextColor1 = Color(0xFFFFFFFF);
const drawerTextColor2 = Color(0xFF000000);

const drawerTileColor1 = Color(0xFF5E5F60);
const drawerTileColor2 = Color(0xFFD9D9D9);

const drawerHoverColor1 = Color(0xFF000000);
const drawerHoverColor2 = Color(0xFFFFFFFF);

const mobileWidth = 800.0;

const aboutMeScript =
    "You can call me JC and I'm a second-year BS Computer Science student at the Technological University of the Philippines Manila. Since I started coding, I've always wanted to be a Software Engineer but now I'm having interests in Web Developing.";
